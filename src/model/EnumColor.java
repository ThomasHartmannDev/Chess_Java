package model;

public enum EnumColor { // Creating the choices of the colors with enum.
    /**
     * This make you never write the wrong color, Java is case-sensitive, so Black is not the same as black, BLACK, or
     * something else.
     */
    BLACK,
    WHITE;
}
